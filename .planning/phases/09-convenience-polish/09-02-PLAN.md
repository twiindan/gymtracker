---
phase: 09-convenience-polish
plan: 02
type: execute
wave: 1
depends_on:
  - 09-01
files_modified:
  - src/app/settings/page.tsx
  - src/components/header.tsx
autonomous: true
requirements:
  - CONV-04

must_haves:
  truths:
    - "Settings page exists at /settings route"
    - "Settings page has JSON and CSV export buttons"
    - "Export buttons trigger client-side data fetch, format, and download"
    - "Header includes Settings navigation link"
  artifacts:
    - path: "src/app/settings/page.tsx"
      provides: "Settings page with export UI"
      min_lines: 80
    - path: "src/components/header.tsx"
      provides: "Updated header with Settings nav link"
  key_links:
    - from: "src/app/settings/page.tsx"
      to: "src/lib/export-utils.ts"
      via: "fetchAllExportData, buildJsonExport, buildCsvExport, downloadFile"
      pattern: "import.*from.*export-utils"
    - from: "src/components/header.tsx"
      to: "src/app/settings/page.tsx"
      via: "NavLink href=/settings"
      pattern: "href=\"/settings\""
---

<objective>
Build the Settings page at `/settings` with JSON and CSV export buttons, and add a Settings link to the header navigation.

Purpose: User-facing export UI — ties together the utility library from Plan 01 into a usable feature.
Output: `src/app/settings/page.tsx` + updated `src/components/header.tsx`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-convenience-polish/09-CONTEXT.md
@.planning/phases/09-convenience-polish/09-01-SUMMARY.md
@src/lib/export-utils.ts
@src/components/header.tsx
@src/app/insights/page.tsx

<interfaces>
From src/lib/export-utils.ts:
```typescript
export async function fetchAllExportData(): Promise<ExportData>
export function buildJsonExport(data: ExportData): string
export function buildCsvExport(data: ExportData): string
export function downloadFile(content: string, filename: string, mimeType: string): void
```

From src/components/header.tsx:
```typescript
export function Header()
function NavLink({ href, icon, label }: { href: string; icon: React.ReactNode; label: string })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create Settings page with export UI</name>
  <files>src/app/settings/page.tsx</files>
  <read_first>
    - src/lib/export-utils.ts — Export functions to call from UI
    - src/app/insights/page.tsx — Pattern for client page structure, loading states, styling
    - .planning/phases/09-convenience-polish/09-CONTEXT.md — Locked decisions D-84 through D-89
  </read_first>
  <action>
Create `src/app/settings/page.tsx` as a client component (`"use client"`). Follow the existing page pattern from `src/app/insights/page.tsx`.

**Page structure:**
- `"use client"` directive at top
- Header section with settings icon (gear/cog SVG) and title "Settings"
- Description: "Export your GymTracker data for backup or analysis."
- Export section card with:
  - Section title: "Data Export"
  - Description: "Download all your workout data in your preferred format."
  - Two export buttons side by side:
    1. **JSON Export** — Primary style, icon: `{}` or code icon, label "Export JSON"
    2. **CSV Export** — Secondary/outline style, icon: table/spreadsheet icon, label "Export CSV"
  - Each button triggers: `fetchAllExportData()` → format with `buildJsonExport` or `buildCsvExport` → `downloadFile` with appropriate filename and MIME type
  - JSON filename: `gymtracker-export-YYYY-MM-DD.json`, MIME: `application/json`
  - CSV filename: `gymtracker-export-YYYY-MM-DD.csv`, MIME: `text/csv`
  - Show loading state on button while export is in progress (disable button, show spinner)
  - Show error state if fetch fails (inline error message below buttons)

**Styling:** Follow existing patterns — `max-w-5xl mx-auto px-4 py-4 space-y-8`, card styling with `rounded-xl border border-border bg-surface`, button styling matching existing app patterns (primary: `bg-zinc-900 text-white dark:bg-zinc-100 dark:text-zinc-900`, secondary: `border border-border`).

**Loading state:** While `fetchAllExportData()` runs, disable both buttons and show a spinner or "Exporting..." text. Use a single `exporting` state that covers both formats (only one export at a time).

**Error handling:** Catch errors from `fetchAllExportData()` and display an inline error message. Reset error state on next export attempt.
  </action>
  <acceptance_criteria>
    - src/app/settings/page.tsx exists as a client component ("use client" directive)
    - Page has a header with "Settings" title and description
    - Page has a "Data Export" section with two buttons: "Export JSON" and "Export CSV"
    - JSON button calls fetchAllExportData → buildJsonExport → downloadFile with .json filename
    - CSV button calls fetchAllExportData → buildCsvExport → downloadFile with .csv filename
    - Buttons show loading state during export (disabled + spinner/text)
    - Error state displays if fetch fails
    - File imports from src/lib/export-utils.ts (grep: "import.*from.*export-utils")
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - Settings page compiles without TypeScript errors
    - Export buttons trigger correct data flow (fetch → format → download)
    - Loading and error states work correctly
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add Settings link to header navigation</name>
  <files>src/components/header.tsx</files>
  <read_first>
    - src/components/header.tsx — Current header structure and NavLink pattern
  </read_first>
  <action>
Add a Settings `NavLink` to the header navigation in `src/components/header.tsx`.

- Add a new `NavLink` with `href="/settings"`, a gear/cog SVG icon, and label "Settings"
- Place it after the existing nav links (after Insights)
- Use the same SVG icon style as existing icons (fill="none", stroke="currentColor", viewBox="0 0 24 24", strokeWidth={2})
- Gear icon path suggestion: `M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.066 2.573c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.573 1.066c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.066-2.573c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z` and inner circle `M15 12a3 3 0 11-6 0 3 3 0 016 0z`
  </action>
  <acceptance_criteria>
    - src/components/header.tsx contains NavLink with href="/settings"
    - Settings link has a gear/cog SVG icon
    - Settings link has label "Settings"
    - Header still compiles and renders correctly
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - Header shows Settings link in navigation
    - TypeScript compilation passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings UI → Export Utils | Trusted internal call — no external input |
| Export Utils → Browser Download | Client-side blob download — no server involvement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Information Disclosure | Settings page export | mitigate | Only exports user's own data (Supabase RLS enforces row-level access); no shared exports |
| T-09-05 | Denial of Service | fetchAllExportData on large datasets | accept | Personal app scale — unlikely to have enough data to cause issues; future optimization if needed |
</threat_model>

<verification>
- TypeScript compilation passes with no errors
- Settings page renders at /settings route
- JSON export button triggers download of .json file
- CSV export button triggers download of .csv file
- Loading state disables buttons during export
- Error state displays on fetch failure
- Header shows Settings nav link
</verification>

<success_criteria>
- src/app/settings/page.tsx exists as client component with export UI
- src/components/header.tsx updated with Settings NavLink
- Both export formats work end-to-end (fetch → format → download)
- Loading and error states provide clear user feedback
</success_criteria>

<output>
After completion, create `.planning/phases/09-convenience-polish/09-02-SUMMARY.md`
</output>
